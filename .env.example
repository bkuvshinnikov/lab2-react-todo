# Copy the Atlas Node.js connection string here and replace its placeholders.
# Percent-encode special characters in the username/password, e.g. @ becomes %40.
# Never prefix these variables with NEXT_PUBLIC_.
MONGODB_URI=
MONGODB_DB=task_manager
